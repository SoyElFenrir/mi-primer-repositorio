// const array=[1,1,1,14,1,1]
// let num1
// let num2
// function sd(array){
//     for(i=0; i<=array.lenght(); i++){
//         num1=array[i]
        
//     }
//     return
// }
// sd(array)
// console.log(num1)

/*const array = ['a', 'b', 'c', 'd']
const newArray = array.slice(1, 2)
console.log('array: ', array)
console.log('array: ', newArray)*/

/*const array=[1,1,5,1,1,1]
function sd(array){
    return array.find(n=> array.indexOf(n) === array.lastIndexOf(n));
}
console.log(sd(array))
*/

/*const array=[]
function Counter() {
    this.sum = 0;
    this.count = 0;
  }
  Counter.prototype.add = function(array) {
    array.forEach(function(entry) {
      this.sum += entry;
      ++this.count;
    }, this);
    // ^---- Note
  };
  
  var obj = new Counter();
  obj.add([2, 5, 9]);
  obj.count
  // 3 
  obj.sum
  // 16*/

  len = O.lenght >>> 0;
